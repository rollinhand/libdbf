# Makefile for dbf, src directory

include ../Makefile.rules

all: $(LIBOBJ) $(GUIOBJ)
	ar rc libdbf.a $(LIBOBJ)
	ranlib libdbf.a

	$(CC) $(CFLAGS) -I$(PWD)/src -L$(PWD)/src -o $(BIN) $(GUIOBJ) -ldbf

clean:
	rm *.o
	rm $(BIN)
	rm libdbf.a
